header.header
    nav.navbar.navbar-expand-lg.fixed-top.py-3
        .container
            p.navbar-brand.text-uppercase.font-weight-bold MINHOBOOK
            button.navbar-toggler.navbar-toggler-right(type='button' data-toggle='collapse' data-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
                i.fa.fa-bars
            #navbarSupportedContent.collapse.navbar-collapse
                ul.navbar-nav.ml-auto
                    li.nav-item.active
                        a.nav-link.text-uppercase.font-weight-bold(href='/home')
                            | Home
                            span.sr-only (current)
                    li.nav-item
                        a.nav-link.text-uppercase.font-weight-bold(href='/account') Perfil
                    if(user.level >=2)
                        li.nav-item
                            a.nav-link.text-uppercase.font-weight-bold(href='/account') Content
                    if(user.level >= 3)
                        li.nav-item
                            a.nav-link.text-uppercase.font-weight-bold(href='/account') Administraçao
                    li.nav-item
                        a.nav-link.text-uppercase.font-weight-bold(href='/users/logout') Logout
