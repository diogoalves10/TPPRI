version: "3"
services:
  minhobook:
    container_name: minhobook
    image: basto/minhobook
    build: .
    ports:
      - "8080:3500"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/db